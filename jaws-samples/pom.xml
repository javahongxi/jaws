<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.hongxi</groupId>
        <artifactId>jaws-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>jaws-samples</artifactId>
    <packaging>pom</packaging>

    <modules>
        <module>jaws-sample-provider</module>
        <module>jaws-sample-consumer</module>
        <module>jaws-sample-api</module>
        <module>jaws-sample-injvm</module>
        <module>jaws-sample-benchmark</module>
        <module>jaws-sample-provider-boot</module>
        <module>jaws-sample-consumer-boot</module>
        <module>jaws-sample-provider-mcp</module>
        <module>jaws-sample-provider-direct</module>
        <module>jaws-sample-consumer-direct</module>
        <module>jaws-sample-provider-http2</module>
        <module>jaws-sample-consumer-http2</module>
        <module>jaws-sample-gray</module>
    </modules>

    <dependencies>
        <!-- samples are runnable applications, provide a logging implementation -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.4</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>