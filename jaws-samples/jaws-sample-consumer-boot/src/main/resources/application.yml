server:
  port: 8081
spring:
  application:
    name: sample-consumer-boot

jaws:
  application:
    name: ${spring.application.name}
  protocol:
    name: jaws
    port: 10000
  registry:
    address: 127.0.0.1
    port: 8848
    protocol: nacos